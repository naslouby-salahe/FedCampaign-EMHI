# Roadmap audit matrix

This matrix is the implementation audit record for `docs/Roadmap.md`. A row is
Supported only when its configured inputs, execution path, persisted evidence,
and tests have been inspected. `In progress` and `Unverified` are not evidence
of scientific completion.

| Roadmap area | Required implementation evidence | Audit status | Evidence or finding |
| --- | --- | --- | --- |
| 4.1–4.8 EMHI core | Detector scores, ranks, context, residual ranks, bounded basis, atoms, calibration, aggregation | In progress | Implementations exist under `src/fedcampaign_emhi/emhi`; real-data execution must be traced end-to-end. |
| 5 detector protocol | Dataset-specific detector fit and score artifacts | In progress | Implementations exist under `detectors`; audit must verify partitions and held-out wiring. |
| 6 raw datasets | Only paths under `data/raw`, released files, provenance inventory | Supported | Canonical TON-IoT and Edge-IIoTSet paths are configured and preprocessing inventories are produced. |
| 7.1 duplicates | Complete retained-record representation and counts | In progress | DuckDB aggregation is executable; duplicate identity must be checked against complete-payload requirement. |
| 7.2 invalid records | Deterministic exclusion reasons and discrepancy manifest | In progress | Counts are persisted; reason-level records need audit. |
| 7.3–7.5 epoch features, scaling, selection | Configured features, training-only scaling, deterministic client selection | Supported | Protocol amendment 1 selects four benign-eligible TON_IoT clients with a shared benign interval; current prepared/split manifests contain all required primary partitions. |
| 8 contexts | Exact, inclusive, leave-one-out, partial, oracle, and diagnostic definitions | In progress | Shared context-member functions are wired; each consumer needs contract coverage. |
| 9 local detectors | IF, robust PCA, autoencoder, calibration and rank conversion | In progress | Source present; not yet execution-audited. |
| 10 methods/comparators | All declared EMHI and comparator methods with lawful contexts | In progress | Method contracts exist. Synthetic contracts without their method-specific evaluator now fail closed rather than emitting generic smoke evidence. |
| 11 metrics | PFA, ODI, stopping, derivatives, attenuation, drift, coverage, runtime | In progress | Derivative/attenuation now persisted for self-explanation; comparator rows now use calibrated immutable local policies and real strict-ODI/lead computations. Broader metric audit remains. |
| 12 fixtures | Exact deterministic fixture outcomes and tolerances | In progress | Fixture modules/tests exist; full fixture evidence audit pending. |
| 13.1 synthetic module validation | Every exact fixture and invariant | Supported | The production-config run completed one validation cell under the active material digest; status reports a valid lifecycle and report materialization emitted its evidence-source table. Unit, scientific, smoke, and end-to-end contracts cover the declared fixture invariants. |
| 13.2 self-explanation | Complete grid per seed, paired trajectories, OLS derivatives, material gates, and primary Holm decision | In progress | `evaluate_self_explanation_seed` materializes 1,260 conditions per seed and runner persists them. Confirmatory multiplicity synthesis remains open. |
| 13.3 pure-order separation | Every generator/effect/order/method cell, subset and target drift | In progress | Grid cells retain their declared mixed-order enabled-term set and source context-dependent triple effects from their dedicated configured parameter. Fitted EMHI artifact grids and native-comparator grids are wired through the execution layer, and only their own missing-grid states can block a cell. Per-cell drift persistence and confirmatory primary synthesis remain unverified. |
| 13.4 HOFD equivalence | Configured support grid, paired CIs, cosine and stopping differences | In progress | The registered producer now fits EMHI and HOFD residuals independently on every configured support level and compares paired held-out atoms. Confirmatory stopping-time equivalence and execution evidence remain outstanding while experiments are paused. |
| 13.5 comparator composition | Candidate error, PFA, runtime and tie-break artifact | Unverified | The one-seed proxy/zero-runtime route and downstream first-candidate fallback are disabled; the complete development-seed selection evaluator remains to be implemented. |
| 13.6 estimator feasibility | Support/context grid and feasibility criteria | In progress | The producer now executes the declared development support/order/basis/context/forced-ridge/forced-no-abstention grid and restricts confirmation to the primary order-three condition. Every condition uses lagged outside-histogram clustering, residual-rank fitting, blocked-CV projection calibration, and a fresh component-seeded evaluation sequence. A typed confirmatory aggregate persists the four feasibility criteria and pooled numerical-failure rate. The implementation is ready for held execution; no confirmatory result exists while experiments are paused. |
| 13.7 sequential evidence | Finite horizon calibration and held-out validity | Implementation ready | The signed-theorem null route has deterministic seed evaluation and confirmatory one-sided BCa synthesis. The operational common-mode route materializes independent nuisance, calibration, and held-out horizons, runs the production fitted EMHI/evidence/calibration path per seed, persists a fail-closed confirmatory PFA synthesis, and has a small-config fitted-path test. No scientific result has been generated while execution is paused. |
| 13.8–13.17 real and robustness experiments | Each declared method/seed/dataset/condition and all outcomes | In progress | Real EMHI and comparator paths now use real local-policy stopping and strict-ODI semantics; complete current evidence remains pending. |
| 14 statistics | Exact sign-flip, BCa, fixed Holm families, Not Tested handling | In progress | Statistics path exists but must be checked against all fixed families and metrics. |
| 16 CLI | Plan, doctor, preprocess, run, status, report and resume semantics | Supported | Production `doctor` validates the configured raw inventories with zero unspecified choices; `plan` materializes the complete declared role/seed schedule and minimum-reconstruction resume sequence. CLI contract and end-to-end tests cover command ownership and incomplete-evidence rejection. |
| 17 lifecycle | Fingerprints, stale descendants, immutable published artifacts | In progress | Source fingerprinting and stale checks exist; lineage audit pending. |
| 18 evidence | Raw, derived, statistical, and manuscript inputs linked to cell records | In progress | Self-explanation cell now includes measurement evidence; broad evidence graph audit pending. |
| 19 completion gates | All requirements, no placeholders, full quality suite, real reruns | Unverified | No completion claim is permitted while any matrix row is open. |
| 20 reports | Evidence tables, explicit states, reproducible sources | In progress | Reports generate; content must be checked after all execution repairs. |

## Repair log

| Date | Area | Change | Verification |
| --- | --- | --- | --- |
| 2026-08-24 | 13.2 | Replaced the self-explanation smoke check with paired common-mode trajectories, the complete configured grid, post-settling OLS derivatives, attenuation, material gates, and persisted measurement evidence. | Focused unit suite and one confirmatory-seed execution. |
| 2026-08-24 | 6–7 | Audited the mounted TON_IoT raw release; the original 12-client selection had no common benign support. Adopted Protocol amendment 1, retaining the benign eligibility gates and choosing the leading four eligible clients. | `fedcampaign preprocess --overwrite`; prepared data, splits, and campaign registry are current under the amended material digest. |
| 2026-08-24 | 17 | Repaired seed derivation so nested component seeds remain serializable under RFC 8785/I-JSON canonicalization. | Runtime seed unit tests and Pyright pass; the primary evaluation was restarted under the repaired code. |
| 2026-08-24 | 13.8, 17 | Added exact trajectory reuse during a single seed/method evaluation, keyed by rank and fit provenance as well as horizon/order. | Focused cache regression test, Ruff, strict Pyright, and formatting pass; no scientific value, threshold, or trajectory definition changes. |
| 2026-08-24 | M08–M10 tracking | Reopened remote M08/M09 milestones and their evidence-dependent issues, plus incomplete M10 synthesis/reporting issues. | Their acceptance criteria require current complete confirmatory artifacts; the prior material digest is stale and the corrected primary run is active. |
| 2026-08-24 | Architecture enforcement | Added the existing Semgrep policy to the required GitHub Actions quality workflow. | Workflow YAML parses and the policy scans all tracked production Python files with zero findings. |
| 2026-08-24 | Architecture enforcement | Rechecked FedACT directly at current remote `main` and added an adapted configuration-value provenance rule. Replaced a hardcoded smoke-gate ridge-candidate grid with the configured candidate grid. | New rule has positive/negative canaries; architecture/evaluation/smoke tests, Ruff, and strict Pyright pass. |
| 2026-08-24 | 13.4–13.7 and boundary contracts | Removed the generic smoke producer that could label distinct synthetic scientific contracts completed without executing their declared methods, grids, or metrics; removed the composition proxy and first-candidate fallback. | Incomplete contracts now persist an explicit invalid diagnostic and cannot create synthetic completion evidence; selected-composition use requires a validated artifact. Focused unit tests, Ruff, and strict Pyright pass. |
| 2026-08-24 | 13.7 operational finite horizon | Added an execution-layer producer for independent common-mode nuisance, calibration, and held-out horizons. It fits the production EMHI hierarchy, uses production finite-horizon calibration and held-out Clopper-Pearson evaluation, and persists a confirmatory aggregation that fails closed if any seed has no available operating point. | Architecture suite, Ruff, strict Pyright, a fail-closed aggregation test, and a small-config fitted production-path test pass. The route has not been executed as a scientific experiment. |
| 2026-08-24 | 13.6 context-support substrate | Implemented the roadmap's deterministic context-support generator with one lag row, exact per-cell usable counts, deterministic outside midpoints, and seeded independent target ranks. | Determinism and exact-count/midpoint tests plus architecture primitive/module checks pass. The test-only artifact bridge was removed; it will be introduced only with the real feasibility executor. |
| 2026-08-24 | 13.6 feasibility executor | Wired the declared development grid and confirmatory primary-order-three path to real lagged outside-context fitting and fresh evaluation, including residual ranks, blocked-CV ridge calibration, component-derived seed streams, and roadmap metrics. Added typed confirmatory aggregation with the four feasibility criteria and pooled numerical-failure rate. | Focused smoke-scale evaluator, grid/aggregation tests, strict Pyright, Ruff, and dependency/test-only-production architecture rules pass. Scientific results remain unexecuted by instruction. |
| 2026-08-24 | 13.3 generator grid fidelity | Repaired pure-order cell modeling so mixed-order enabled-term sets and the context-dependent triple's dedicated effect parameter survive enumeration into purity validation. Added exact rejection samplers for the declared mixed-order and state-conditioned triple laws. | Focused generator, strict Pyright, Ruff, and architecture tests pass. Method-specific fitted scoring remains open. |
| 2026-08-24 | 13.3 evidence integrity | Removed a direct generator-coordinate drift result that had been labeled as Full EMHI despite not fitting/scoring that method. | Pure-order cells now persist an explicit missing-fitted-evaluator diagnostic and cannot create a confirmatory statistic until the declared evaluator is implemented. |
